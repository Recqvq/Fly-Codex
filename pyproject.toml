[project]
name = "flyingcodex"
version = "0.2.0"
description = "Feishu to Codex bridge with JSON route config"
requires-python = ">=3.11"
dependencies = [
  "lark-oapi>=1.4.16",
  "loguru>=0.7.2",
]

[tool.uv]
package = false
